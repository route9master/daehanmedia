# DESIGN.md — 대한미디어그룹 (daehanmedia.kr)

> 이 문서가 유일한 디자인 판단 기준이다. 여기 없는 색·폰트·효과는 쓰지 않는다.
> 판단이 애매하면 "이게 이 브리프만의 선택인가, 아무 사이트에나 쓸 기본값인가"를 먼저 묻는다.

---

## 0. 디자인 논지 (Thesis)

**"광고 지면 도감(圖鑑)"**

식물도감이나 조류도감을 펼쳤을 때의 판면을 웹으로 옮긴다.
표제어가 크게 박히고, 아래에 정의가 붙고, 옆에 분류 기호가 달린 그 형식.
이 사이트는 75개 광고 상품을 **도감의 표제어**로 다룬다.

핵심은 **한 항목이 한 화면을 쓴다**는 것. 카드 그리드도, 표 격자도 없다.
스크롤하면 도감을 넘기듯 항목이 지나가고, 좌측에는 색인이 계속 따라붙는다.
정보를 빽빽하게 채워 넣지 않는 그 여유가 곧 가격대를 말한다.

여기에 로고의 **월계관 = 검인(檢印)** 이라는 두 번째 층을 얹는다. 도감의 표본에는
채집자의 인장이 찍힌다. 스크롤이 각 분류(門)에 닿을 때 월계관이 인주색으로 찍힌다.

### 절대 하지 않는 것
가격·단가·비용 관련 표기는 사이트 어디에도 없다. 숫자는 오직 **지면의 크기**(도달 규모)와
**분류 번호**로만 등장한다. "얼마"라는 단어가 화면에 뜨는 순간 도감이 아니라 카탈로그가 된다.

### 금지 (AI가 만든 티가 나는 기본값)
- ❌ 보라/파랑 그라데이션, 글래스모피즘, 네온 글로우
- ❌ Inter · Pretendard · Poppins · Montserrat · Space Grotesk
- ❌ 크림색(#F4F1EA) 배경 + 테라코타(#D97757) 액센트 조합 — 요즘 AI가 제일 많이 뱉는 조합
- ❌ 새까만 배경 + 형광 라임/버밀리언 액센트 단일 조합
- ❌ 카드 안에 카드, 라운드 16px 카드 그리드, 카드 hover 시 3D 틸트
- ❌ 바운스/일래스틱 이징, 요소마다 흩뿌린 fade-up
- ❌ `01 / 02 / 03` 넘버링을 장식으로 쓰기 (**순서가 실제 정보일 때만** 사용 — 프로세스 페이지만 허용)
- ❌ 이모지 아이콘, 무료 아이콘팩 라인 아이콘 남발

---

## 1. 색 (Color Tokens)

인쇄와 관인(官印)의 팔레트. 크림+테라코타를 피하고, **단청(丹靑)의 뇌록색**을 신호색으로 쓴다.
뇌록은 이 사이트의 예상 밖 지점이다. 데이터·수치 영역에서만 등장한다.

```css
:root {
  /* 지면 */
  --paper:        #E4E1D6;  /* 갱지. 크림보다 채도 높고 살짝 어둡다 */
  --paper-warm:   #EDEAE1;  /* 밝은 면, 카드 대신 쓰는 지면 분절 */
  --paper-edge:   #CFCBBD;  /* 헤어라인, 표 괘선 */

  /* 먹 */
  --ink:          #191713;  /* 완전 검정 아님. 갈색기 있는 먹 */
  --ink-deep:     #0E0D0B;  /* 반전 지면(먹지) 배경 */
  --ink-mute:     #6B665C;  /* 보조 텍스트 */

  /* 인주 — 도장, 유일한 강조 */
  --seal:         #A62B24;
  --seal-wash:    #A62B2412;

  /* 뇌록 — 데이터/신호 전용. 그 외 사용 금지 */
  --sig:          #4E6156;
  --sig-bright:   #7A9384;

  /* 황동 — 수치, 단위, 통계 */
  --brass:        #8A6B2E;
}
```

**사용 비율 규칙:** 지면 70 / 먹 22 / 뇌록 5 / 인주 2 / 황동 1.
인주가 화면 면적의 3%를 넘으면 잘못 쓰고 있는 것이다.

---

## 2. 타이포그래피

### 폰트 3종 + 시그니처 1종

| 역할 | 폰트 | 라이선스 | 비고 |
|---|---|---|---|
| 국문 디스플레이 | **본명조 (Source Han Serif KR)** Heavy / Bold | SIL OFL | 관보·증서의 무게. 마루부리는 너무 부드러워서 탈락 |
| 국문 본문 | **Wanted Sans** Regular / Medium / SemiBold | SIL OFL | Pretendard 대체. 자소가 더 넓고 중성적 |
| 데이터·라벨 | **Geist Mono** | SIL OFL | 숫자, 과금방식, 지면 코드 |
| **시그니처** | **Redaction 100 / 70 / 35 / 10** | SIL OFL | 라틴 전용. 인쇄 해상도 단계별 4종 |

### Redaction — 이 사이트의 타이포 시그니처

Redaction은 인쇄 열화 정도가 다른 4개 버전이 한 세트인 폰트다.
**섹션 영문 제목(SEARCH / DISPLAY / VIDEO / COMMERCE / GROWTH / BRAND)은
뷰포트 진입 시 Redaction 100(가장 거친 인쇄) → Redaction 10(선명)으로 전환된다.**
0.7초, `cubic-bezier(.2,.7,.1,1)`. 마치 인쇄기가 초점을 잡는 것처럼 보인다.

이건 다른 광고대행사 사이트에 없다. **사이트당 이 효과는 섹션 제목에만 쓴다.** 남발 금지.

### 타입 스케일

```
display-1   본명조 Heavy   clamp(3.4rem, 8vw, 7.5rem)   ls -0.03em  lh 1.02
display-2   본명조 Bold    clamp(2.2rem, 4.5vw, 4rem)   ls -0.02em  lh 1.12
eyebrow     Geist Mono     0.75rem  ls 0.22em  uppercase
row-title   Wanted Sans SemiBold  1.25rem  lh 1.35   ← 단가표 행 제목
body        Wanted Sans Regular   1rem     lh 1.75
data        Geist Mono     0.8125rem  ls 0.04em
```

국문 본문 자간은 `-0.01em`. 행간 1.75 아래로 내리지 않는다.

---

## 3. 레이아웃 — 도감 판면

카드 그리드를 쓰지 않는다. 12칼럼 부트스트랩 격자도 쓰지 않는다.
**한 항목 = 한 판면**이 전 페이지의 뼈대다.

```
┌────────┬──────────────────────────────────────────────────────┐
│        │  S-04                            ← 분류기호 (mono)    │
│  색    │                                                       │
│  인    │  네이버 파워링크                  ← 표제어 (본명조)    │
│  (고정)│  ──────────────────────────                          │
│        │                                                       │
│  S-01  │  검색창에 무언가를 적어 넣은 사람은                    │
│  S-02  │  이미 마음을 절반쯤 정한 사람입니다.                   │
│ ▸S-04  │  그 순간 가장 위에 있는 것.        ← 도감 문구         │
│  S-05  │                                                       │
│        │  어울리는 곳 · 과금 방식           ← 각주 (mono)       │
└────────┴──────────────────────────────────────────────────────┘
   200px                    minmax(0, 1fr)
```

- **판면 최소 높이 `min(78vh, 640px)`.** 한 화면에 두 항목이 동시에 보이지 않게 한다.
  빽빽함이 곧 저가의 신호다. 비워두는 것이 이 사이트의 값이다.
- 괘선은 표제어 아래 **한 줄만**. `1px solid var(--paper-edge)`, 폭은 표제어 너비의 40%.
- `border-radius: 0` — 전역. 단 하나의 예외도 없음.
- 좌측 색인은 `position: sticky`. 현재 항목만 `--seal` 색 + 앞에 `▸`. 나머지는 `--ink-mute` 0.45 투명도.
- 스크롤로 항목이 바뀔 때: 표제어가 **아래에서 12px 올라오며 opacity 0→1**, 220ms.
  도감 문구는 40ms 늦게 따라온다. 그게 전부다. 다른 요소는 움직이지 않는다.
- 색인 항목 클릭 → 해당 판면으로 스크롤. 페이지 리로드 없음.

### 표(table)를 쓰는 유일한 곳
`/media` 지면 도감. 여기만 행 구조가 정보로서 의미가 있다. 그 외 표 금지.

### 컨테이너
`max-width: 1480px`, 좌우 패딩 `clamp(20px, 5vw, 96px)`.
섹션 상하 여백 `clamp(96px, 14vh, 200px)`.

---

## 4. 시그니처 요소 — 검인(檢印)

사이트 전체에서 **6번만** 등장한다. 각 서비스 축의 섹션 헤더에 1번씩.

1. 섹션이 뷰포트 55%에 진입
2. 월계관 심볼(`dmg-symbol.svg`)이 `--seal` 색으로, 투명도 0 → 0.9
3. 동시에 `scale(1.14) → scale(1)`, `rotate(-4deg) → rotate(-1.5deg)`
4. 지속 **180ms**, `cubic-bezier(.15,1.1,.3,1)` — 도장이 눌리는 속도
5. 착지 직후 4px 반경의 미세한 잉크 번짐 (`filter: blur(0.4px)` 60ms 후 해제)

각 섹션마다 **회전각이 -4°, +2°, -1°, +3°, -2°, +1°로 다르다.** 손으로 찍은 도장은 매번 다르니까.
이 불규칙성이 "사람이 만들었다"는 신호를 만든다.

---

## 5. 모션 원칙

- **이징은 딱 2개만.** `cubic-bezier(.2,.7,.1,1)` (기본), `cubic-bezier(.15,1.1,.3,1)` (검인 전용).
  바운스·일래스틱 금지.
- **스크롤 라이브러리:** Lenis (`lerp: 0.085`). 관성 과하게 주지 않는다.
- **애니메이션 라이브러리:** Motion (`npm i motion`). GSAP 안 씀.
- 페이지 진입 시퀀스: 지면이 **위→아래로 인쇄되듯** 한 번만 스캔 (0.9초).
  로고 심볼이 한 획씩 그려지는 로더는 **첫 방문 세션에만** (sessionStorage), 재방문 시 생략.
- 페이지 전환: 먹지(`--ink-deep`)가 위에서 내려와 덮고, 다음 페이지가 지면색으로 인쇄되어 나타난다.
  덮기 340ms / 벗기 420ms.
- `prefers-reduced-motion: reduce` → 모든 transform·스캔·검인 제거, opacity 전환만 유지.
- **금지:** 패럴랙스 배경, 마우스 따라다니는 blob, 스크롤 진행바(진행바는 단가표 페이지에만 허용).

---

## 6. 커서

기본 커서를 숨기지 않는다 (접근성). 대신 **도감 판면에서만** 커서 옆에
`Geist Mono 11px`로 현재 항목의 분류기호가 따라붙는다 (예: `S-04`). 지연 없음, 즉시 추종.
원형 blob 커서 금지.

---

## 7. 로고 사용 규칙

| 파일 | 용도 |
|---|---|
| `dmg-lockup-horizontal.svg` | 헤더, 푸터 기본 |
| `dmg-lockup-vertical.svg` | 명함, OG 이미지, 로딩 화면 |
| `dmg-symbol.svg` | 검인 효과, 파비콘, 워터마크 |
| `dmg-wordmark.svg` | 심볼이 이미 근처에 있을 때 |
| `*-white.svg` | 먹지 배경 위 |

- 최소 보호 여백: 심볼 높이의 **0.4배**
- 헤더 로고 높이: 데스크톱 30px / 모바일 24px
- 심볼을 늘리거나, 색을 두 개 이상 쓰거나, 그림자를 넣지 않는다
- 워터마크로 쓸 때 투명도 최대 **0.05**

---

## 8. 품질 하한선 (announce 하지 말고 그냥 지킬 것)

- 모바일 375px까지 깨지지 않음
- 키보드 포커스 링 보임 (`outline: 2px solid var(--seal); outline-offset: 3px`)
- 명도 대비: 본문 4.5:1 이상, 큰 글씨 3:1 이상
- LCP 2.0s 이하 — 히어로에 무거운 영상 대신 타입+캔버스
- 폰트는 `font-display: swap` + subset (한글 서브셋 필수, 본명조 원본은 20MB급)
- 이미지 `loading="lazy"`, WebP/AVIF

---

## 9. 빌드 제약 (가비아 웹호스팅)

- **Astro 정적 빌드** → `dist/` 를 FTP 업로드. SSR 없음.
- 문의 폼: `/api/contact.php` (PHPMailer) + MySQL 저장. Node 서버 없음.
- 배포: GitHub Actions `SamKirkland/FTP-Deploy-Action`
- 모든 인터랙션은 클라이언트 사이드. 서버 렌더링 의존 금지.
